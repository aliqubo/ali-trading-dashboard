"""Password security package (ARCHITECTURE.md §9.1).

Reconstructed MVP scaffolding — original source unavailable. Nothing else in
this MVP imports from this package's root (all imports use full submodule
paths, e.g. `from app.core.security.password_hasher import PasswordHasher`),
so no re-exports are declared here.
"""

from __future__ import annotations
